---
phase: 02-desktop-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/dashboard/SessionFilter.tsx
  - src/components/dashboard/VisitList.tsx
  - src/components/dashboard/VisitDetail.tsx
  - src/components/dashboard/AudioPlayer.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "User can view a chronological list of all visits with address, time, and session color"
    - "User can click any visit in the list to see its detail view"
    - "User can play back the audio recording from the detail view"
    - "User can filter both map and list by selecting a canvassing session"
    - "Selecting 'All Sessions' shows every visit"
  artifacts:
    - path: "src/components/dashboard/SessionFilter.tsx"
      provides: "Session selector dropdown with All Sessions option"
      min_lines: 20
    - path: "src/components/dashboard/VisitList.tsx"
      provides: "Scrollable chronological visit list with session color badges"
      min_lines: 40
    - path: "src/components/dashboard/VisitDetail.tsx"
      provides: "Selected visit detail panel with address, timestamp, and audio player"
      min_lines: 30
    - path: "src/components/dashboard/AudioPlayer.tsx"
      provides: "Audio playback component using signed download URLs"
      min_lines: 25
  key_links:
    - from: "src/components/dashboard/AudioPlayer.tsx"
      to: "src/actions/storage.ts"
      via: "calls createSignedDownloadUrl server action on play"
      pattern: "createSignedDownloadUrl"
    - from: "src/components/dashboard/VisitList.tsx"
      to: "src/components/dashboard/DashboardShell.tsx"
      via: "props: visits, sessionColorMap, selectedVisitId, onSelectVisit"
      pattern: "onSelectVisit"
    - from: "src/components/dashboard/SessionFilter.tsx"
      to: "src/components/dashboard/DashboardShell.tsx"
      via: "props: sessions, selectedSessionId, onSelectSession"
      pattern: "onSelectSession"
---

<objective>
Implement the right panel: session filter dropdown, chronological visit list, visit detail view with audio playback.

Purpose: Makes recorded visits browsable and playable — the user can filter by session, scan a list of visits, and listen to any recording with full metadata context.
Output: Four working components replacing stubs from Plan 01: SessionFilter, VisitList, VisitDetail, AudioPlayer.
</objective>

<execution_context>
@/Users/caldayham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caldayham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-desktop-dashboard/02-RESEARCH.md
@.planning/phases/02-desktop-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- Props interfaces established by Plan 01 stubs -->

SessionFilter props:
```typescript
{
  sessions: Session[];
  selectedSessionId: string | null;
  onSelectSession: (id: string | null) => void;
}
```

VisitList props:
```typescript
{
  visits: Visit[];
  sessionColorMap: Map<string, string>;
  selectedVisitId: string | null;
  onSelectVisit: (id: string) => void;
}
```

VisitDetail props:
```typescript
{
  visit: Visit | null;
  sessionColor: string;
}
```

AudioPlayer props:
```typescript
{
  audioPath: string;
  mimeType: string | null;
}
```

From src/actions/storage.ts:
```typescript
export async function createSignedDownloadUrl(filePath: string): Promise<string>;
```

From src/lib/types.ts:
```typescript
export type Session = {
  id: string;
  user_id: string;
  label: string | null;
  started_at: string;
  ended_at: string | null;
  notes: string | null;
};

export type Visit = {
  id: string;
  session_id: string;
  latitude: number;
  longitude: number;
  address: string | null;
  audio_path: string | null;
  audio_mime_type: string | null;
  audio_duration_seconds: number | null;
  result: string | null;
  recorded_at: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SessionFilter and VisitList components</name>
  <files>src/components/dashboard/SessionFilter.tsx, src/components/dashboard/VisitList.tsx</files>
  <action>
1. Replace stub `src/components/dashboard/SessionFilter.tsx`:
   - 'use client' directive
   - Import `Session` from `@/lib/types`, `format` from `date-fns`, `Filter` icon from `lucide-react`
   - Render a `<div className="p-3 border-b border-zinc-800 flex items-center gap-2">`:
     - `<Filter size={16} className="text-zinc-400 shrink-0" />`
     - `<select>` element with dark styling: `className="flex-1 bg-zinc-800 text-white text-sm rounded px-2 py-1.5 border border-zinc-700 focus:outline-none focus:border-blue-500"`
     - First option: `<option value="">All Sessions</option>`
     - Map sessions to options: `<option value={session.id}>{session.label || format(new Date(session.started_at), 'MMM d, yyyy h:mm a')}</option>`
     - `onChange` handler: call `onSelectSession(e.target.value || null)` (empty string → null for "All Sessions")
     - `value={selectedSessionId ?? ''}`

2. Replace stub `src/components/dashboard/VisitList.tsx`:
   - 'use client' directive
   - Import `Visit` from `@/lib/types`, `format` from `date-fns`, `MapPin`, `Clock`, `Mic` icons from `lucide-react`
   - Render `<div className="flex-1 overflow-y-auto">`:
     - If visits.length === 0: render centered empty state `<p className="text-zinc-500 text-sm text-center p-4">No visits to show</p>`
     - Map visits to clickable cards: `<button key={visit.id} onClick={() => onSelectVisit(visit.id)} className="w-full text-left p-3 border-b border-zinc-800/50 hover:bg-zinc-800/50 transition-colors {visit.id === selectedVisitId ? 'bg-zinc-800' : ''}">`
     - Each card shows:
       - Top row: small colored dot (`<span className="w-2.5 h-2.5 rounded-full shrink-0" style={{ backgroundColor: sessionColorMap.get(visit.session_id) ?? '#3B82F6' }} />`) + address (or "Unknown address") in `text-sm text-white truncate` + if visit.audio_path, a `<Mic size={14} className="text-zinc-500 shrink-0" />`
       - Bottom row: `<Clock size={12} className="text-zinc-500" />` + formatted timestamp `format(new Date(visit.recorded_at), 'MMM d, h:mm a')` in `text-xs text-zinc-500` + visit.result badge if present (small text with subtle background)
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/simple-conversation-location-tracker && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>SessionFilter renders a dropdown with "All Sessions" + per-session options. VisitList renders a scrollable chronological list with session color dots, addresses, timestamps, and audio indicators. Both components type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement VisitDetail and AudioPlayer components</name>
  <files>src/components/dashboard/VisitDetail.tsx, src/components/dashboard/AudioPlayer.tsx</files>
  <action>
1. Replace stub `src/components/dashboard/AudioPlayer.tsx`:
   - 'use client' directive
   - Import `useState` from React, `createSignedDownloadUrl` from `@/actions/storage`, `Play`, `Loader2`, `AlertCircle` icons from `lucide-react`
   - State: `audioUrl: string | null`, `loading: boolean`, `error: string | null`
   - On mount or when `audioPath` changes (useEffect): reset state (null URL, no error, not loading). Do NOT auto-fetch the URL — fetch on demand.
   - `handlePlay` async function:
     - If `audioUrl` already set, do nothing (audio element handles playback)
     - Set `loading = true`, `error = null`
     - Try: call `createSignedDownloadUrl(audioPath)`, set `audioUrl` to result
     - Catch: set `error` to message, show toast via `sonner`
     - Finally: set `loading = false`
   - Render:
     - If `error`: `<div className="flex items-center gap-2 text-red-400 text-sm"><AlertCircle size={14} /> Failed to load audio <button onClick={handlePlay} className="underline">Retry</button></div>`
     - If `audioUrl`: `<audio controls src={audioUrl} className="w-full h-8" autoPlay />` (autoPlay on first load so clicking "play" immediately starts)
     - Otherwise: `<button onClick={handlePlay} className="flex items-center gap-2 text-sm text-blue-400 hover:text-blue-300">` with Play icon or Loader2 (animate-spin) when loading, text "Load audio" / "Loading..."

2. Replace stub `src/components/dashboard/VisitDetail.tsx`:
   - 'use client' directive
   - Import `Visit` from `@/lib/types`, `format` from `date-fns`, `MapPin`, `Clock`, `Tag` icons from `lucide-react`, `AudioPlayer` from `./AudioPlayer`
   - If `visit` is null, return null
   - Render `<div className="border-t border-zinc-800 p-4 space-y-3 shrink-0">`:
     - Header: `<h3 className="text-white font-medium text-sm truncate">` with visit.address or "Unknown address"
     - Color indicator: small bar/line with `sessionColor` background at top or beside the header
     - Metadata grid (text-xs text-zinc-400):
       - `<MapPin size={12} />` + `${visit.latitude.toFixed(5)}, ${visit.longitude.toFixed(5)}`
       - `<Clock size={12} />` + `format(new Date(visit.recorded_at), 'EEEE, MMM d, yyyy h:mm a')`
       - If visit.result: `<Tag size={12} />` + visit.result
       - If visit.audio_duration_seconds: duration formatted as `Xm Ys` or `Xs`
     - Audio section: if `visit.audio_path`, render `<AudioPlayer audioPath={visit.audio_path} mimeType={visit.audio_mime_type} />`; otherwise show `<p className="text-zinc-500 text-xs">No audio recorded</p>`
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/simple-conversation-location-tracker && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>VisitDetail shows address, coordinates, timestamp, result tag, and duration for selected visit. AudioPlayer fetches signed URL on demand and renders native audio element for playback. Error handling with retry is implemented. All type-check cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Session filter dropdown lists all sessions plus "All Sessions" default
3. Selecting a session filters the visit list (and map, via shared state in DashboardShell)
4. Visit list shows chronological entries with session color dots, addresses, timestamps
5. Clicking a visit in the list shows its detail panel
6. Detail panel displays address, coordinates, timestamp, and result
7. Clicking "Load audio" in detail panel fetches a signed URL and plays the recording
8. Audio playback error shows retry button
</verification>

<success_criteria>
- Session filter controls both map and list via shared state
- Visit list is scrollable and shows all relevant metadata per entry
- Visit detail shows full information including coordinates and result
- Audio playback works via on-demand signed URL fetching
- Empty states are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-dashboard/02-03-SUMMARY.md`
</output>
