---
phase: 02-desktop-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/layout.tsx
  - src/app/dashboard/page.tsx
  - src/actions/dashboard.ts
  - src/actions/storage.ts
  - src/lib/colors.ts
  - src/components/dashboard/DashboardShell.tsx
  - src/components/dashboard/DashboardMap.tsx
  - src/components/dashboard/VisitList.tsx
  - src/components/dashboard/VisitDetail.tsx
  - src/components/dashboard/SessionFilter.tsx
  - src/components/dashboard/AudioPlayer.tsx
autonomous: true
requirements:
  - DASH-06

must_haves:
  truths:
    - "Navigating to /dashboard renders a two-panel desktop layout without errors"
    - "Server component fetches sessions and visits from Supabase and passes them to client shell"
    - "Signed download URL server action exists and returns a valid URL for audio files"
    - "Session color utility assigns deterministic colors to sessions"
  artifacts:
    - path: "src/app/dashboard/layout.tsx"
      provides: "Full-viewport dashboard layout with header and flex main area"
    - path: "src/app/dashboard/page.tsx"
      provides: "Server component data fetching for sessions and visits"
    - path: "src/actions/dashboard.ts"
      provides: "getDashboardData server action"
      exports: ["getDashboardData"]
    - path: "src/actions/storage.ts"
      provides: "createSignedDownloadUrl server action added alongside existing upload action"
      exports: ["createSignedUploadUrl", "createSignedDownloadUrl"]
    - path: "src/lib/colors.ts"
      provides: "SESSION_COLORS palette and getSessionColor utility"
      exports: ["SESSION_COLORS", "getSessionColor"]
    - path: "src/components/dashboard/DashboardShell.tsx"
      provides: "Client component managing selected visit, session filter, and layout split"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/actions/dashboard.ts"
      via: "direct import of getDashboardData"
      pattern: "getDashboardData"
    - from: "src/components/dashboard/DashboardShell.tsx"
      to: "src/components/dashboard/DashboardMap.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*DashboardMap"
---

<objective>
Dashboard foundation: desktop layout, server-side data fetching, and component shell with stubs for map, list, detail, filter, and audio player.

Purpose: Establishes the /dashboard route, data pipeline, and shared state management so Wave 2 plans can implement map and list/detail components in parallel without file conflicts.
Output: Working /dashboard page with two-panel layout rendering placeholder components, plus server actions for data and audio URL generation.
</objective>

<execution_context>
@/Users/caldayham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caldayham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-dashboard/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan uses -->

From src/lib/types.ts:
```typescript
export type Session = {
  id: string;
  user_id: string;
  label: string | null;
  started_at: string;
  ended_at: string | null;
  notes: string | null;
};

export type Visit = {
  id: string;
  session_id: string;
  latitude: number;
  longitude: number;
  address: string | null;
  audio_path: string | null;
  audio_mime_type: string | null;
  audio_duration_seconds: number | null;
  transcript: string | null;
  notes: string | null;
  result: string | null;
  recorded_at: string;
  created_at: string;
};
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>;
```

From src/actions/storage.ts (existing):
```typescript
export async function createSignedUploadUrl(filePath: string): Promise<{ signedUrl: string; path: string; token: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard server actions, color utility, and signed download URL</name>
  <files>src/actions/dashboard.ts, src/actions/storage.ts, src/lib/colors.ts</files>
  <action>
1. Create `src/actions/dashboard.ts` — a 'use server' module exporting `getDashboardData()`:
   - Import `createClient` from `@/lib/supabase/server`
   - Call `supabase.auth.getUser()`, throw if not authenticated
   - Use `Promise.all` to fetch both `sessions` (ordered by `started_at` desc) and `visits` (ordered by `recorded_at` desc)
   - Return `{ sessions: Session[], visits: Visit[] }` (import types from `@/lib/types`)

2. Add `createSignedDownloadUrl` to existing `src/actions/storage.ts`:
   - 'use server' already declared
   - Accept `filePath: string` parameter
   - Auth check via `getUser()`
   - Call `supabase.storage.from('audio').createSignedUrl(filePath, 3600)` (1 hour TTL)
   - Return `data.signedUrl` string
   - Throw on error with descriptive message

3. Create `src/lib/colors.ts`:
   - Export `SESSION_COLORS` array: ['#3B82F6', '#EF4444', '#10B981', '#F59E0B', '#8B5CF6', '#EC4899', '#06B6D4', '#F97316']
   - Export `getSessionColor(sessionIndex: number): string` — returns `SESSION_COLORS[sessionIndex % SESSION_COLORS.length]`
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/simple-conversation-location-tracker && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Three files exist: dashboard.ts exports getDashboardData, storage.ts has both upload and download URL actions, colors.ts exports palette and getter function. All type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard layout, page, shell, and stub components</name>
  <files>src/app/dashboard/layout.tsx, src/app/dashboard/page.tsx, src/components/dashboard/DashboardShell.tsx, src/components/dashboard/DashboardMap.tsx, src/components/dashboard/VisitList.tsx, src/components/dashboard/VisitDetail.tsx, src/components/dashboard/SessionFilter.tsx, src/components/dashboard/AudioPlayer.tsx</files>
  <action>
1. Create `src/app/dashboard/layout.tsx`:
   - Default export function `DashboardLayout({ children })`
   - Render: `<div className="h-screen flex flex-col">` containing:
     - `<header className="h-14 border-b border-zinc-800 bg-zinc-950 flex items-center justify-between px-4 shrink-0">` with `<h1 className="font-semibold text-white">Canvassing Dashboard</h1>` and a link to `/record` (use `lucide-react` Map icon + "Record" text)
     - `<main className="flex-1 flex overflow-hidden">{children}</main>`
   - Use dark theme classes consistent with the existing app (bg-zinc-950, text-white)

2. Create `src/app/dashboard/page.tsx` (server component):
   - Import `getDashboardData` from `@/actions/dashboard`
   - Import `DashboardShell` (not dynamic — it's the client boundary)
   - Call `getDashboardData()` in the async component body
   - Render `<DashboardShell sessions={sessions} visits={visits} />`

3. Create `src/components/dashboard/DashboardShell.tsx` ('use client'):
   - Props: `{ sessions: Session[], visits: Visit[] }`
   - State: `selectedVisitId: string | null`, `selectedSessionId: string | null` (null = all sessions)
   - Compute `filteredVisits` from visits filtered by selectedSessionId (null means show all)
   - Compute `sessionColorMap`: a `Map<string, string>` mapping each session.id to a color via `getSessionColor(index)` where index is the session's position in the sessions array
   - Use `next/dynamic` to import `DashboardMap` with `{ ssr: false, loading: () => <div className="flex-1 bg-zinc-900 animate-pulse" /> }`
   - Layout: two-column flex — left side (`<div className="flex-1 relative">`) renders DashboardMap, right side (`<div className="w-[400px] border-l border-zinc-800 flex flex-col overflow-hidden">`) renders SessionFilter at top, then VisitList in scrollable area, then VisitDetail at bottom if a visit is selected
   - Pass to DashboardMap: `visits={filteredVisits}`, `sessionColorMap`, `selectedVisitId`, `onSelectVisit={(id) => setSelectedVisitId(id)}`
   - Pass to SessionFilter: `sessions`, `selectedSessionId`, `onSelectSession={(id) => setSelectedSessionId(id)}`
   - Pass to VisitList: `visits={filteredVisits}`, `sessionColorMap`, `selectedVisitId`, `onSelectVisit`
   - Pass to VisitDetail: `visit={selectedVisit}` (found from visits by selectedVisitId), `sessionColor` (from sessionColorMap)
   - Handle empty state: if visits.length === 0, show centered message "No visits recorded yet. Start a canvassing session from your phone."

4. Create stub components (each is a minimal 'use client' component that renders a placeholder div — Wave 2 plans will replace these with real implementations):

   `src/components/dashboard/DashboardMap.tsx`:
   - Props: `{ visits: Visit[], sessionColorMap: Map<string, string>, selectedVisitId: string | null, onSelectVisit: (id: string) => void }`
   - Render: `<div className="w-full h-full bg-zinc-900 flex items-center justify-center text-zinc-500">Map loading...</div>`

   `src/components/dashboard/VisitList.tsx`:
   - Props: `{ visits: Visit[], sessionColorMap: Map<string, string>, selectedVisitId: string | null, onSelectVisit: (id: string) => void }`
   - Render: `<div className="flex-1 overflow-y-auto p-2 text-zinc-500">Visit list placeholder</div>`

   `src/components/dashboard/VisitDetail.tsx`:
   - Props: `{ visit: Visit | null, sessionColor: string }`
   - Render: if visit is null, render nothing; otherwise `<div className="p-4 border-t border-zinc-800 text-zinc-500">Detail placeholder</div>`

   `src/components/dashboard/SessionFilter.tsx`:
   - Props: `{ sessions: Session[], selectedSessionId: string | null, onSelectSession: (id: string | null) => void }`
   - Render: `<div className="p-2 border-b border-zinc-800 text-zinc-500">Filter placeholder</div>`

   `src/components/dashboard/AudioPlayer.tsx`:
   - Props: `{ audioPath: string, mimeType: string | null }`
   - Render: `<div className="text-zinc-500">Audio player placeholder</div>`
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/simple-conversation-location-tracker && npx tsc --noEmit --pretty 2>&1 | head -30 && curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/dashboard 2>/dev/null || echo "Dev server not running — type check passed"</automated>
  </verify>
  <done>Navigating to /dashboard renders a dark two-panel layout with header, placeholder map on the left, and placeholder list/filter/detail on the right. No TypeScript errors. All stub components accept their final prop signatures so Wave 2 plans only need to replace component internals.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `/dashboard` route renders without crashing (server component fetches data successfully)
3. Two-panel layout visible: map area left (~60%), list+detail area right (~40%)
4. All 6 dashboard component files exist with correct prop types
5. `getDashboardData` returns sessions and visits arrays
6. `createSignedDownloadUrl` exists in storage.ts alongside upload action
7. `getSessionColor` returns deterministic colors
</verification>

<success_criteria>
- /dashboard route exists and renders a full-viewport two-panel layout
- Server component successfully fetches sessions and visits from Supabase
- All dashboard components are created with their final prop interfaces
- Color utility and signed download URL action are ready for use by Wave 2 plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-dashboard/02-01-SUMMARY.md`
</output>
